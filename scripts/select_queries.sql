
-- SELECT запросы для получения всех строк 
-- каждой сущности данной базы данных

 -- SELECT запрос для получения списка всех продавцов:
SELECT * FROM marketplace_database.seller;

 -- SELECT запрос для получения списка всех товаров:
SELECT * FROM marketplace_database.product;

 -- SELECT запрос для получения списка всех покупателей:
SELECT * FROM marketplace_database.customer;

 -- SELECT запрос для получения списка всех заказов:
SELECT * FROM marketplace_database."order";

 -- SELECT запрос для получения списка всех возможных статусов заказов:
SELECT * FROM marketplace_database.order_status;

 -- SELECT запрос для получения списка всех корзин заказов:
SELECT * FROM marketplace_database.cart;

 -- SELECT запрос для получения списка всех служб доставки:
SELECT * FROM marketplace_database.delivery_service;

 -- SELECT запрос для получения списка
 -- всех оставленных комментариев о продуктах:
SELECT * FROM marketplace_database.product_comment;

-----------------------------------------------------------------------

